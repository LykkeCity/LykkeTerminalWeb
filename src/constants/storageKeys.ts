const keys = {
  baseAsset: 'lwt-baseAsset',
  layout: 'lwt-layout',
  notificationId: 'lwt-notificationId',
  selectedInstrument: 'selected-instrument',
  token: 'lwt-token'
};

export default keys;
